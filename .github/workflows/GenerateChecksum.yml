name: Generate md5 sums
on: [push]
jobs:
  Generate-Sums:
    runs-on: ubuntu-20.04
    steps:
    - name: Generate sums
      run: |
         sudo mkdir /work
         cd /work
         sudo wget -q https://git.kernel.org/torvalds/t/linux-5.19-rc6.tar.gz
         md5sum *tar.xz
         sha256sum *tar.xz
    - name: Upload Kernel
      uses: actions/upload-artifact@v2
      with:
        name: Kernel
        path: /work/*.tar.xz
